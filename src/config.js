const DefaultConfig = {
    Application: {
        appName: 'TAR Framework v1' // set Application name
    },
    config: {
        colorMode: 'Light', // Light, Dark
    },
    lang: {
        defaultLang: 'id' // set default languange for all component
    }
}

export default DefaultConfig